# 注意事项

## Git 与测试

1. 每个逻辑完整的改动都必须创建独立 Git commit，以便后续追踪和回滚。
2. 行为改动必须编写或更新相关测试；纯文档或工作流改动必须更新对应验证方式。
3. commit 前只执行受影响范围的检查，不要求为每个 commit 重复运行完整门禁。
4. 一次交付中的所有 commit 完成后，必须运行一次 `pnpm verify:full`，且全部通过后才能交付。
5. 全量门禁失败时，先修复并运行失败项及受影响检查；确认稳定后再重新运行 `pnpm verify:full`。

## Agent 协作

1. 文案、局部样式和单组件修改默认由单个实现 Agent 完成。
2. 游戏规则、数据库、Socket.IO、重连、事务、并发、安全或跨包改动必须增加独立验证 Agent。
3. 实现 Agent 默认使用 Sol medium；复杂跨层问题可升级为 Sol high。
4. 验证 Agent 默认使用 Luna medium 并保持只读，不得修改实现、测试或降低断言；只有疑难竞态、幂等或 flaky 问题才升级为 Luna max。
5. 同一时间只能有一个 Agent 修改工作区；不得并行运行会共享服务或数据库的集成测试、负载测试。
6. 验证失败时，验证 Agent 应报告首个失败命令、关键日志、影响范围与环境判断，并将修复工作交还实现 Agent。

详细验证矩阵和交接格式见 `docs/verification-workflow.md`。
